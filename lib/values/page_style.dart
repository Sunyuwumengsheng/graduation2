import 'package:flutter/material.dart';

final textLable = TextStyle(

    fontFamily: 'OnenSans',
    fontWeight: FontWeight.bold
);
final textContainer = BoxDecoration(
  color: Color(0xFF6CA8F1),
  borderRadius: BorderRadius.circular(10.0),
  boxShadow: [
    BoxShadow(
      color: Colors.black12,
      blurRadius: 6.0,
      offset: Offset(0, 2),
    ),
  ],
);
final texthnit_style = TextStyle(
    color: Colors.white24,
    fontFamily: 'OpenSans'
);
